<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowContentTransitions">true</item>
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/paulsneutral</item>
        <item name="colorPrimary">@color/paddysblue</item>
        <item name="android:textColorPrimary">@color/rachelsblack</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/switchfonts</item>
    </style>

    <style name="ActionBarTheme" parent="Widget.AppCompat.Toolbar">
        <item name="selectableItemBackground">?android:selectableItemBackground</item>
        <item name="selectableItemBackgroundBorderless">?android:selectableItemBackground</item>
    </style>

    <style name="SwitchStyle" parent="Theme.AppCompat.Light">
        <item name="android:fontFamily">@font/switchfonts</item>
        <item name="colorControlActivated">@color/rachelsdarkprink</item>
        <item name="colorSwitchThumbNormal">@color/paddysblue</item>
        <item name="android:colorForeground">@color/paddysblue</item>
    </style>

</resources>
